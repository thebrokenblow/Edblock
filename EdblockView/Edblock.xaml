<Window x:Class="EdblockView.Edblock"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:components="clr-namespace:EdblockView.Components"
        mc:Ignorable="d"
        Title="Edblock" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Canvas 
                Width="140" 
                Height="60" 
                Background="#FF52C0AA">
                <TextBox Cursor="Hand" Text="Some Text" Height="60" Width="140" TextAlignment="Center" Focusable="False" Background="Transparent" Foreground="White"/>

            </Canvas>
        </StackPanel>
        <components:CanvasSymbols Grid.Column="1"/>
    </Grid>
</Window>
