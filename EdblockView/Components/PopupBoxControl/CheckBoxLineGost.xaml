<UserControl x:Class="EdblockView.Components.PopupBoxControl.CheckBoxLineGost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:componentsVM="clr-namespace:EdblockViewModel.ComponentsVM;assembly=EdblockViewModel" 
             d:DataContext="{d:DesignInstance Type=componentsVM:CheckBoxLineGostVM}"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="255">
    <DockPanel>
        <!--<materialDesign:PackIcon
            DockPanel.Dock="Left"
            Style="{StaticResource IconSettingsMenu}"
            Kind="FileDocument" />-->

        <Label 
            Margin="5 0 5 0"
            HorizontalContentAlignment="Center"
            Style="{StaticResource LabelSettingsMenu}"
            Content="Вход, выход линий по ГОСТ" />

        <CheckBox
            IsChecked="{Binding IsDrawingLinesAccordingGOST}"
            Height="40"
            DockPanel.Dock="Right"
            Style="{StaticResource CheckBoxSettingsMenu}"/>
    </DockPanel>
</UserControl>