<UserControl x:Class="EdblockView.SymbolsView.ActionSymbolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:symbolView="clr-namespace:EdblockView.SymbolsView" 
             xmlns:symbolVM="clr-namespace:EdblockViewModel.Symbols;assembly=EdblockViewModel" 
             d:DataContext="{d:DesignInstance Type=symbolVM:ActionSymbolVM}"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="140">

    <Border Style="{StaticResource BorderSymbolView}">
        <Canvas
            Width="{Binding Width}" 
            Height="{Binding Height}" 
            Background="{Binding Color}">
            
            <symbolView:TextFieldView />
            <symbolView:ConnectionPointsView />
            <symbolView:ScaleRectanglesView />
        </Canvas>
    </Border>
</UserControl>