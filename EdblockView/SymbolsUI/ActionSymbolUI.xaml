<UserControl
    x:Class="EdblockView.SymbolsUI.ActionSymbolUI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:edblockviewmodel="clr-namespace:EdblockViewModel;assembly=EdblockViewModel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=edblockviewmodel:EdblockVM}"
    d:DesignHeight="80"
    d:DesignWidth="360"
    mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <Canvas
            Width="140"
            Height="60"
            Margin="10,10,10,10"
            Background="#FF52C0AA">
            <TextBlock
                Width="140"
                Height="60"
                Style="{StaticResource TextFieldUI}"
                Text="Действие" />
        </Canvas>

        <Label Content="Действие (1)" Style="{StaticResource LabelSymbolMenu}" />
    </StackPanel>
</UserControl>