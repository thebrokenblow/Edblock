<UserControl x:Class="EdblockView.SymbolsUI.CycleWhileBeginSymbolUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="350">
    <StackPanel Orientation="Horizontal">
        <Canvas
            Style="{StaticResource FrameSymbol}">
            <Polygon 
                Fill="#CCCCFF" 
                Points="0 60 0 10 10 0 130 0 140 10 140 60"/>

            <TextBlock
                Text="Начало цикла"
                Height="60"
                Width="140"
                Style="{StaticResource TextFieldUI}"/>
        </Canvas>

        <Label
            Margin="10 0 0 0"
            Content="Цикл (9) do while, while (Начало)"
            Style="{StaticResource LabelSymbolMenu}" />
    </StackPanel>
</UserControl>